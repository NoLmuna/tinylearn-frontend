# Features Directory

This directory contains feature-specific components and logic organized by feature.

## Guidelines

- Each feature should have its own subdirectory
- Feature directories can contain components, hooks, and utils specific to that feature
- Features are more complex than simple components and may have their own state management

## Example Structure

```
features/
  counter/
    Counter.jsx
    useCounter.js
    counterUtils.js
```
